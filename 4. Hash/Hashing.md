## Hashing

Hashing is a technique in Data Structures and Algorithms (DSA) used to store and retrieve data quickly. It allows operations like searching, inserting, and deleting to be performed in O(1) (constant time) on average.

In python hashing can be done with dictionary.